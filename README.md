# CNN
## CNN projects:
### handwriiten digit
Training the model with handwritten digit pictures in MNIST repository, the model after trainning can classify handwritten digit with 99% accuracy.
### animal sound classification
Training the model with UrbanSound8K raw data. Preprocess the data first, then train the model with the data after preprocessing.The model after trainning can classify animal sound in UrbanSound8K with 90% accuracy.
Reference:https://urbansounddataset.weebly.com/urbansound8k.html
