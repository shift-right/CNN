# CNN
## CNN projects:
### handwriiten digit
Train the model with handwritten digit picture in MNIST repository, the model after trainning can classify handwritten digit in 99% accuracy.
### animal sound classification
Train the model with UrbanSound8K raw data. Preprocess the data first, then train the model with the data after preprocessing.The model after trainning can classify animal sound in 90% accuracy.
Reference:https://urbansounddataset.weebly.com/urbansound8k.html
